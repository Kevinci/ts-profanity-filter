node_modules/
dist/
*.tgz
.DS_Store

# Local secrets — never committed
.env
.env.local
.env.*.local

# IDE
.idea/
*.iml

# Output from local CLI runs in the repo root. Deliberately not `*.ndjson`:
# the docs recommend keeping a fixtures/*.ndjson in a repo for CI, and a blanket
# rule would ignore it silently.
/report.pdf
/flagged.ndjson
/all.ndjson
